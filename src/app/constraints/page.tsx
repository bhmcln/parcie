import ConstraintsPanel from "@/components/constraints-panel";

export const metadata = {
  title: "Constraints · parcie",
};

export default function ConstraintsPage() {
  return (
    <div className="max-w-3xl mx-auto px-6 py-8">
      <ConstraintsPanel />
    </div>
  );
}
